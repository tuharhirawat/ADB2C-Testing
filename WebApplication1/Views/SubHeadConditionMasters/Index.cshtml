@model IEnumerable<SubHeadConditionMaster>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MainHeadId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxPage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExtraRate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MainHeadId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxPage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtraRate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
